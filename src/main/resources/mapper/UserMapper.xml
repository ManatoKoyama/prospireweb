<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.prospire.prospireweb.repository.UserMapper">
  
  <resultMap type="com.prospire.prospireweb.model.User" id="user"><!--  id属性は任意の値 -->
  	<!--  columnはSQLのカラム　propertyはエンティティクラスのフィールド -->
  	<result column="username" property="username"/>
  	<result column="password" property="password"/>
    <result column="authority" property="role"/><!--  カラムauthorityをroleフィールドにマッピング -->
  </resultMap>
 
    <select id="findByUsername" resultType="com.prospire.prospireweb.model.User">
        SELECT
            username,
            password,
            authority As role
        FROM
            mst_user 
        WHERE
            username = #{username}
    </select>

  </mapper>