<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.prospire.prospireweb.repository.UserMapper">
  
  <resultMap type="com.prospire.prospireweb.model.User" id="user"><!--  id属性は任意の値 -->
  	<!--  columnはSQLのカラム　propertyはエンティティクラスのフィールド -->
  	<result column="username" property="username"/>
  	<result column="password" property="password"/>
    <result column="authority" property="role"/><!--  カラムauthorityをroleフィールドにマッピング -->
  </resultMap>
 
    <select id="findByUsername" resultType="com.prospire.prospireweb.model.User">
        SELECT
            username,
            password,
            authority As role
        FROM
            mst_user 
        WHERE
            username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.prospire.prospireweb.model.User">
        INSERT INTO mst_user (username, kanji_name_1, kanji_name_2, kana_name_1, kana_name_2, role, organization, mail_adress, authority, password, password_expiration)
        VALUES (#{username}, #{kanjiName1}, #{kanjiName2}, #{kanaName1}, #{kanaName2}, #{role}, #{organization}, #{mailAddress}, #{role}, #{password}, CURRENT_TIMESTAMP)
    </insert>

    <update id="updateUser" parameterType="com.prospire.prospireweb.model.User">
        UPDATE mst_user
        SET
            password = #{password},
            authority = #{role}
        WHERE
            username = #{username}
    </update>

    <delete id="deleteUser" parameterType="String">
        DELETE FROM mst_user
        WHERE username = #{username}
    </delete>

  </mapper>